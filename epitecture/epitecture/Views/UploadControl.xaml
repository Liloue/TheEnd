<Page
    x:Class="epitecture.Views.UploadControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:epitecture.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="Upload Image : " Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" FontFamily="Calibri"/>
        <TextBox Text="{Binding ImagePath, Mode=TwoWay}" HorizontalAlignment="Stretch" Grid.Column="1"  FontFamily="Calibri"/>
        <Button Content="Browse" Command="{Binding OnBrowserButton}" Grid.Column="2" FontFamily="Calibri"/>
        <Button Content="Upload" Command="{Binding OnUploadButton}" Grid.Column="3" FontFamily="Calibri" />
    </Grid>
</Page>
